import styled from 'styled-components';

export const AdminLoading = styled.div`
    width: 100vw;
    height: 100vh;

    position: fixed;
    top: 0;
    left: 0;
    z-index: 13000;

    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;

    background-color: rgba(0, 0, 0, 0.45);
`;
